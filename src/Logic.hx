import generated.page0.Parameter0;

class Logic {
    public static function hello() {
        var param = new Parameter0();
        
        //param. <- [ctrl + space]
        
        trace("hello");
    }
}